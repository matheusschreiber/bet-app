import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    fontFamily: "montserratBold",
  },
  groupsContainer: {
    width: "90%",
    marginTop: 20,
  },
});
